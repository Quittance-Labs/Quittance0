# Structured logging

This guide moved.

The implemented observability contract (correlation ids, event taxonomy,
allow-listed fields, and privacy rules) lives in [`docs/LOGGING.md`](./LOGGING.md).
Event names and field allow-lists are defined in
`backend/src/observability/log-events.ts`.

Do not add a second overlapping logging guide.
