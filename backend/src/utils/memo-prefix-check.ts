import { INVOICE_MEMO_PREFIX, hasInvoiceMemoPrefix } from '../../../shared/memo';

export { INVOICE_MEMO_PREFIX, hasInvoiceMemoPrefix };

export default {
  hasInvoiceMemoPrefix,
  INVOICE_MEMO_PREFIX,
};
