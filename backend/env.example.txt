# Server Configuration
PORT=3001
NODE_ENV=development

# Database
# db/schema.sql defines the invoices table with full parity columns:
#   seller_name, seller_email, asset_code, asset_issuer, customer_name,
#   customer_email, payer_public_key, payer_name, payer_email,
#   expires_at, paid_at, metadata (JSONB).
#
# Both server.ts (Postgres) and server-mvp.ts (in-memory) expose the same
# invoice shape through the shared InvoiceStorage interface.
DATABASE_URL=postgresql://user:password@localhost:5432/stellar_invoices

# Stellar Network (Validated by /api/ready)
# Readiness fails fast with 503 if STELLAR_NETWORK is not TESTNET or PUBLIC,
# or if STELLAR_HORIZON_URL does not use HTTPS in production.
STELLAR_NETWORK=TESTNET
# TESTNET or PUBLIC
STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org

# Optional: Enable lightweight Horizon reachability check on readiness probes.
# Default: false (off for demo deploys so external latency does not trigger false negatives)
HEALTH_HORIZON_PING=false

# Optional. Sellers are identified by their connected Freighter wallet; these
# keys only enable the Horizon payment monitor for one fixed account.
SELLER_PUBLIC_KEY=
SELLER_SECRET_KEY=

# Cursor-based Horizon monitor. Lower values detect payment faster but consume
# more Horizon requests. A file checkpoint is available for no-Postgres runs.
PAYMENT_MONITOR_POLL_MS=5000
# PAYMENT_MONITOR_CURSOR_FILE=.data/payment-monitor-cursors.json

# Redis (for Bull queue)
REDIS_HOST=localhost
REDIS_PORT=6379

# Frontend URL & CORS (Validated by /api/ready)
# Readiness returns 503 in production if no frontend origin is configured.
# Liveness (/api/health) returns 200 regardless to prevent cold-start restart loops.
FRONTEND_URL=http://localhost:3000
# Optional comma-separated additional exact origins (for previews/custom domain)
FRONTEND_URLS=

# Local-only fake payment endpoint. Production code forces this off regardless.
# Readiness fails fast with 503 if set to true in production.
ALLOW_SIMULATE=false

# Optional: Email/SMS notifications
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
